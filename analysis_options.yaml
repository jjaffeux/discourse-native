include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    - build/**
    - third_party/**
    # These have independent package configs and analyzer gates.
    - packages/discourse_resenha/**
    - profiles/**
    - android/**
    - ios/**
    - web/**
    - windows/**
    - macos/**
    - linux/**
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true

linter:
  rules:
    avoid_dynamic_calls: true
    avoid_type_to_string: true
    cancel_subscriptions: true
    close_sinks: true
    collection_methods_unrelated_type: true
    control_flow_in_finally: true
    deprecated_member_use_from_same_package: true
    directives_ordering: true
    discarded_futures: true
    hash_and_equals: true
    invalid_case_patterns: true
    literal_only_boolean_expressions: true
    no_self_assignments: true
    prefer_void_to_null: true
    secure_pubspec_urls: true
    strict_top_level_inference: true
    test_types_in_equals: true
    throw_in_finally: true
    unawaited_futures: true
    unnecessary_await_in_return: true
    unnecessary_statements: true
    unrelated_type_equality_checks: true
    # `use_decorated_box` is deliberately absent. A bordered `BoxDecoration`
    # reports the border's dimensions as padding, which a `Container` applies
    # and a `DecoratedBox` does not — so the rule's fix silently moves the
    # child under its own border. It is safe only for an unbordered
    # decoration, which is not a distinction a lint can make.
    prefer_const_constructors: true
    prefer_const_constructors_in_immutables: true
    prefer_const_literals_to_create_immutables: true
    prefer_const_declarations: true
    avoid_unnecessary_containers: true
    sized_box_for_whitespace: true
    use_colored_box: true
    sort_child_properties_last: true
    use_string_buffers: true
