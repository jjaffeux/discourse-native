/// Class name accepted by Discourse's bookmark and flag write APIs.
const chatMessageWireType = 'Chat::Message';

/// Polymorphic name persisted and serialized for a Chat message bookmark.
const chatMessagePolymorphicWireType = 'ChatMessage';
